@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

<h1>Cover Type</h1>
<div>
    <a asp-controller="CoverType" asp-action="Create">Create new CoverType</a>
</div>
<table class="table table-bordered table-stripped" style="width:100%">
    <thead>
        <tr>
            <th>CategoryName</th>
            <th>DisplayOrder</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td width="50%">@obj.Name</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a class="btn btn-primary mx-2" asp-route-id="@obj.Id" asp-action="Edit"
                        asp-controller="CoverType"><i class="bi bi-pencil-square"></i>Edit</a>
                        <a class="btn btn-danger" asp-route-id="@obj.Id" asp-action="Delete" asp-controller="CoverType"><i
                            class="bi bi-trash-fill"></i>Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
